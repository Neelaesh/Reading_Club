/**
 * @fileoverview Barrel export for CustomDialog component
 * @description Provides a clean import interface for the CustomDialog component and its types
 */
export { default } from "./CustomDialog";
export type {
  CustomDialogProps,
  ConfirmationDialogProps,
  ContentDialogProps,
  UnifiedDialogProps,
} from "./CustomDialog.types";
