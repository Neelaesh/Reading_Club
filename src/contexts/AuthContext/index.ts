/**
 * @fileoverview Barrel export for AuthContext Provider component
 * @description Provides a clean import interface for the AuthContext Provider and its hooks
 */
export { AuthProvider, useAuth } from "./AuthContext";
