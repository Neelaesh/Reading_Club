import { ReactNode } from "react";

/**
 * @interface MemberCountProviderProps
 * @description Props interface for MemberCountProvider
 */
export interface MemberCountProviderProps {
  children: ReactNode;
}
